# --------------------------------------------------------------
# Developer + training requirements. Includes everything in
# requirements.txt plus the libs needed for data generation,
# experiment tracking, testing, and notebook work.
#
# DO NOT `pip freeze >> requirements-dev.txt`. Transitive deps
# belong in a lock file, not here. If you need a lock, run
# `pip-compile` or `uv pip compile` and commit the .lock output.
# --------------------------------------------------------------
-r requirements.txt

# --- Chart rendering & data wrangling ---
# (render_charts.py uses raw matplotlib for speed; no mplfinance dependency.)
# (pandas-ta intentionally omitted — the ViT pipeline learns from candles directly,
#  not from hand-engineered TA indicators. Add it back if you ever build a hybrid.)
pandas>=2.2
matplotlib>=3.9
seaborn>=0.13
pyarrow>=17.0

# --- Training ---
scikit-learn>=1.5
tqdm>=4.66
datasets>=3.0
accelerate>=1.0

# --- Experiment tracking & data versioning ---
mlflow>=2.17
dvc[gdrive]>=3.55

# --- Testing & linting ---
pytest>=8.3
pytest-cov>=5.0
pytest-asyncio>=0.24
ruff>=0.7
mypy>=1.13
pre-commit>=4.0

# --- Notebooks ---
jupyter>=1.1
ipykernel>=6.29
